# kosmicapp.com

Static marketing site for Kosmic (SparQan LLC). Files live in `site/`.

Deploys automatically to Cloudflare Pages (project `kosmic-website`) on every push to `main`
via `.github/workflows/deploy.yml`. Custom domains: kosmicapp.com, getkosmic.com (redirects).
